# Original CartPole Materials

`Cartpole.ipynb` is the main original notebook linked from the public pages. The `original-materials` folder preserves every supplied source file, including duplicate notebook copies, the original article draft, and the supplied JavaScript and CSS drafts.
